version: '3.4'

services:
  backend:
    build:
      context: ./backend
      target: dev
    image: knuffon-backend:dev
    ports:
      - 8000:8000
    volumes:
      - ./backend:/code
  frontend:
    build:
      context: ./frontend
    image: knuffon-frontend:dev
    ports:
      - 8080:8080
    volumes:
      - ./frontend:/code
